from enum import Enum

class Action(Enum):
    SEARCH = "search"
    LOGIN = "login"
    START = "start"
    JOIN = "join"
    BEGIN = "begin"
    MOVE = "move"
    QUIT = "quit"
    BOARD = "board"
    ERROR = "error"